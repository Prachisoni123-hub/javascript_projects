A simple, elegant, and responsive calculator built with HTML, CSS, and JavaScript.
Supports basic arithmetic operations: addition, subtraction, multiplication, division, and includes clear (AC) and delete (DEL) functionality.

📋 Features

✅ Addition, subtraction, multiplication (×), and division (÷)

✅ Clear (AC) and delete (DEL) buttons

✅ Right-aligned input display

✅ Responsive and visually appealing design

✅ Supports keyboard-like interaction through buttons

✅ Handles errors gracefully (invalid expressions show "Error")

🎨 User Interface

Modern neumorphic design

Poppins font from Google Fonts

Circular, touch-friendly buttons

Dark-themed gradient background

⚙️ Technologies Used

HTML5 – Structure of calculator

CSS3 – Styling and layout

JavaScript (ES6) – Calculator logic and event handling

Google Fonts (Poppins) – Clean, modern typography

🛠️ How It Works

User Input:

Click buttons to form an expression (1+2, 5×4, 10÷2)

Display Update:

JavaScript updates the input field in real-time

Calculation:

When = is pressed:

× is converted to *

÷ is converted to /

eval() computes the result

Error Handling:

Invalid inputs display "Error" and reset the input
